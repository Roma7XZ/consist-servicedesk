server {
    listen 80;
    server_name _;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # Главное правило для Vue Router
        try_files $uri $uri/ /index.html;
    }
}